/**
 * DEPRECATED — Use scripts/seedYouTube.ts instead.
 * This file is kept as a stub to avoid breaking any old imports.
 * The real seeder is in: backend/scripts/seedYouTube.ts
 */
export async function seedData() {
  console.warn(
    "⚠️  src/seedYouTube.ts is deprecated. Run: npx ts-node scripts/seedYouTube.ts"
  );
}
