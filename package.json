{
  "name": "mashhoor-root",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"cd ai-service && .venv\\\\Scripts\\\\python.exe main.py\"",
    "test": "npm run test --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  }
}
